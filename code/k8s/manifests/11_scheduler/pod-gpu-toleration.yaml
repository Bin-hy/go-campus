apiVersion: v1
kind: Pod
metadata:
  name: gpu-workload
spec:
  containers:
    - name: app
      image: nginx:1.27
  # 模拟"AI 推理 Pod"：容忍 GPU 专用节点的污点
  tolerations:
    - key: "gpu"
      operator: "Exists"
      effect: "NoSchedule"
  nodeSelector:
    gpu: "true"
