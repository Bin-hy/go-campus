# 架构师修炼栏目 Checklist（验收记录）

> 验收时间：2026-09-16。全部项目实际执行命令验证，✓ 表示通过。

## 实现完整性

- [x] 栏目首页存在且含 mermaid 路线图（验证：`docs/架构师修炼/index.md` 181 行，含六道坎演进图 + 能力阶梯图 + 统一结构图，构建渲染通过）
- [x] 20 个文档全部存在，文件名与 `plan.md` 一致（验证：`ls docs/架构师修炼/*.md | wc -l` = 20，含首页；`ls .vitepress/dist/架构师修炼/*.html | wc -l` = 20）
- [x] 每篇含导航引用块 + 「这篇解决什么问题」（验证：正则 `^> 属于「架构师修炼」` 19/19 命中，`**这篇解决什么问题**` 19/19 命中）
- [x] 每篇 ≥2 个 mermaid（验证：全栏目 57 张 mermaid 图，最少的 12/13/14/17 各 2 张）
- [x] 每篇 ≥3 张对照表（验证：全栏目 296 张表，逐文件 ≥3）
- [x] 每篇含「故障与一致性边界」（验证：19/19 命中，均为表格形式并明确丢数据/重复窗口）
- [x] 每篇含「面试追问链」（带答案）与「自测清单」（复选）（验证：19/19 命中）
- [x] 代码围栏成对（验证：逐文件 ``` 计数为偶数，脚本检查 19/19 通过）

## 内容质量

- [x] 量级估算可核对（验证：`00` 篇 3.3 节算例与 `15` 篇复用同一口径；GPU 量纲已统一为「2,083 卡·天 = 5 万卡时/天」）
- [x] 跨量级横向对照表存在（验证：`01` 篇第四节「同一件事在六个量级下的做法」覆盖缓存/DB/一致性/MQ/锁/限流/文件/部署/监控/发布/成本 11 个关注点）
- [x] 数据分级贯穿全栏目（验证：`01` 篇 P0~P4 分级表被 `08`/`09`/`11` 篇引用为共同前提）
- [x] 原理不重复、只做链接（验证：抽查 `04`/`10` 篇，原理段落均以 `/后端技术栈强化/...` 链接收口）

## 代码层（`code/architect/`）

- [x] `docker compose config -q` 通过（验证：exit 0）
- [x] `docker compose --profile cluster config -q` 通过（验证：exit 0）
- [x] 服务覆盖实验需求（验证：MySQL 主从 3306/3307、Redis 主从 6380/6381、哨兵 26379-26381、etcd 三节点 12379-12381、Kafka 9092 与 cluster profile 19092-19094）
- [x] 关键参数与文档结论一一对应（验证：`master.cnf` 的 GTID/ROW/`sync_binlog=1`/半同步 timeout、`replica.cnf` 的 `replica_parallel_workers=4`+LOGICAL_CLOCK、`redis-master` 的 `min-replicas-to-write 1`；README「配置要点」逐条给出理由）
- [x] 初始化脚本可执行（验证：`scripts/init-replication.sh` 权限 755，含等待就绪 → GTID 自动定位 → 状态校验 → 提示输出）
- [x] README 含端口表 / 常用命令 / 故障注入速查 / 记录模板（验证：`code/architect/README.md` 各节齐全）

## 站点集成

- [x] nav 新增「架构师修炼」下拉（19 项）（验证：`config.mts` 编译通过，构建产物导航含全部 19 条）
- [x] sidebar 注册 `/架构师修炼/`（分组：开始 / A 方法论 / B 演进六阶段 / C 一致性 / D 稳定性 / E 综合实战 / 相关链接）（验证：`dist/架构师修炼/index.html` 中解析出 19 条栏目链接）
- [x] `docs/index.md` 新增 feature 卡片与推荐顺序第 14 条（验证：构建通过）
- [x] `docs/后端技术栈强化/index.md` 新增「下一步：架构师修炼」映射表并关闭 backlog（验证：构建通过，站内链接无死链）
- [x] 根 `README.md` 学习阶段表新增一行（验证：文件已更新）

## 构建与死链

- [x] `npm run docs:build` 成功（验证：`build complete in 22.52s`，exit 0；剩余告警为既有的 artalk 注释与 chunk 体积提示，与本次改动无关）
- [x] 栏目内相对链接（`./文件名`）全部命中现有文件（验证：脚本扫描 19 个文件，缺失 = 无）
- [x] 站内绝对链接 `/架构师修炼/...` 全部命中（验证：脚本扫描含跨栏目引用共 12 个目标，失效 = 无）
- [x] 代码块语言均有高亮支持（验证：首轮构建告警 `language 'conf' is not loaded`，已将 `04` 篇该围栏改为 `ini`，二次构建无此告警）

## 遗留说明（非阻塞）

- 12 篇 401 行、15 篇 500 行，略高于写作规范建议的 400 行上限（内容为要点密度所致，非冗余）。
- `code/architect/` 仅做了 `docker compose config` 语法校验；镜像拉取与端到端跑通需在本机执行 `docker compose up -d && bash scripts/init-replication.sh`（网络与镜像源相关，未在本次验收中实际拉取）。
- 实验手册 15 个实验的"预期现象"基于机制推演与公开参数给出，实际数值以本机实操记录为准（文档已要求每个实验记录实测数据）。
