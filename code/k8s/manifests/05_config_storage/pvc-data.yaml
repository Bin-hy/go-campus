apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # 不指定 storageClassName：使用集群默认 StorageClass（minikube 自带 standard，动态供给）
